# Application Settings
ENVIRONMENT=production
PORT=10000
DEBUG=False
PROJECT_NAME=PulseQ Backend

# ⚠️ SECURITY: Generate a strong SECRET_KEY for production
# Run this command to generate: python -c "import secrets; print(secrets.token_hex(32))"
# Or use the one below (but generate your own for production!)
SECRET_KEY=smart-token-247bb

# Database Configuration
DB_HOST=your-database-host
DB_NAME=your-database-name
DB_USER=your-database-user
DB_PASSWORD=your-database-password
DB_PORT=5432

# CORS Settings
WEB_BASE_URL=https://your-frontend-url.com
MOBILE_BASE_URL=https://your-mobile-app.com
ALLOWED_ORIGINS=https://pulseq-3l03vs19z-aimens-projects-ff0f0a5e.vercel.app/

# ⚠️ JWT Settings (REQUIRED - at least one must be set)
# The app will use SECRET_KEY first, then fall back to JWT_SECRET_KEY
JWT_SECRET_KEY=5aa92bffdd6147e9a0c7524d66d32a5ffcd1214e05f3af65aed52de04b90c1da
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=1440  # 24 hours

# AI Model Paths (relative to project root)
MODEL_PATH=models/opd_wait_time_model.pkl
ENCODER_PATH=models/label_encoders.pkl

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json  

# Rate Limiting (requests per minute)
RATE_LIMIT=100
